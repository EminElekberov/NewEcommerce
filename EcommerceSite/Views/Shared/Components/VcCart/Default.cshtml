<div class="dropdown cart-dropdown">
    <a href="#" class="dropdown-toggle" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-display="static">
        <div class="icon">
            <i class="icon-shopping-cart"></i>
            @if (ViewBag.cart != null)
            {
                <span class="cart-count">@ViewBag.cart.Count</span>

            }
        </div>
        <p>Cart</p>
    </a>

    <div class="dropdown-menu dropdown-menu-right">
        <div class="dropdown-cart-products">
            @if (ViewBag.cart != null)
            {
                @foreach (var item in ViewBag.cart)
                {
                    <div class="product">
                        <div class="product-cart-details">
                            <h4 class="product-title">
                                <a href="product.html">@item.Product.Name</a>
                            </h4>

                            <span class="cart-product-info">
                                <span class="cart-product-qty">@item.Quantity</span>
                                x $ @item.Product.PresentPrice
                            </span>
                        </div><!-- End .product-cart-details -->

                        <figure class="product-image-container">
                            <a href="product.html" class="product-image">
                                <img src="~/images/categories/@item.Product.Image" alt="product">
                            </a>
                        </figure>
                        <a asp-controller="cart" asp-action="remove" asp-route-id="@item.Product.Id" class="btn-remove" title="Remove Product"><i class="icon-close"></i></a>
                    </div><!-- End .product -->

                }

            }

        </div><!-- End .cart-product -->

        <div class="dropdown-cart-total">
            <span>Total</span>
            @if (ViewBag.total != null)
            {
                <span class="cart-total-price">$@ViewBag.total</span>
            }
            else
            {
                <span class="cart-total-price">$0</span>
            }
        </div><!-- End .dropdown-cart-total -->

        <div class="dropdown-cart-action">
            <a href="cart.html" class="btn btn-primary">View Cart</a>
            <a href="checkout.html" class="btn btn-outline-primary-2"><span>Checkout</span><i class="icon-long-arrow-right"></i></a>
        </div><!-- End .dropdown-cart-total -->
    </div><!-- End .dropdown-menu -->
</div><!-- End .cart-dropdown -->
